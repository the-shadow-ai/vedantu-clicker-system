# =============================================================================
#  Vedantu Clicker System — .gitignore
# =============================================================================

# ── Build outputs ─────────────────────────────────────────────────────────────
build/
out/
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
*.ninja
.ninja_deps
.ninja_log

# ── MSVC / Visual Studio ──────────────────────────────────────────────────────
*.sln
*.vcxproj
*.vcxproj.filters
*.vcxproj.user
.vs/
*.user
*.suo
*.aps
*.pdb
*.ilk
*.exp
*.lib
*.pch
*.idb
ipch/

# ── Compiled binaries ─────────────────────────────────────────────────────────
*.exe
*.dll
*.obj
*.o

# sdk_embed/ binaries ARE intentionally tracked — they are embedded as RCDATA
# in resource.rc at build time so no SDK folder is needed at deployment.
# The !sdk_embed/ override below must come AFTER the *.dll / *.exe rules.
!sdk_embed/
!sdk_embed/**

# ── Release artefacts (generated by post-build step) ─────────────────────────
AppRelease/

# ── NuGet / external package caches (not used in this project) ────────────────
packages/
*.nupkg

# ── Secrets — NEVER commit ────────────────────────────────────────────────────
.env

# ── ImGui saved layout (generated at runtime) ─────────────────────────────────
imgui.ini

# ── Logs ──────────────────────────────────────────────────────────────────────
logs/
*.log
*.csv

# ── vcpkg ─────────────────────────────────────────────────────────────────────
# vcpkg_installed/ is generated during build. Do NOT commit it.
vcpkg_installed/
# The sdk_embed/ binaries ARE committed (they are the pre-staged SDK files
# read by resource.rc at build time). Do not gitignore them.

# ── Node / ui-dashboard ───────────────────────────────────────────────────────
ui-dashboard/node_modules/
ui-dashboard/dist/
ui-dashboard/.env
ui-dashboard/.env.local

# ── OS artefacts ──────────────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store
ehthumbs.db

# ── Editor configs ────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
